{
  "name": "gif_tv",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js --exec babel-node",
    "gif-scan": "nodemon dir-scanner --exec babel-node",
    "serve": "node dist/server.js",
    "buildServer": "babel lib -d dist",
    "build:html": "cp -R src/html/ public/html/",
    "watch:html": "nodemon -e html -x 'npm run build:html'",
    "build:css": "node-sass --include-path scss src/scss/style.scss public/css/main.css",
    "watch:css": "nodemon -e scss -x 'npm run build:css'",
    "build:js": "babel src -d public",
    "watch:js": "nodemon -x 'npm run build:js'",
    "build:assets": "cp -R src/assets/ public/assets/",
    "watch:assets": "onchange 'src/assets/**/*.*' -v -- npm run build:assets",
    "build": "npm run build:html && npm run build:css && npm run build:js && npm run build:assets",
    "build:watch": "parallelshell 'npm run watch:html' 'npm run watch:css' 'npm run watch:js' 'npm run watch:assets'",
    "clean": "rm -r public/*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-preset-env": "^1.4.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.24.1",
    "mocha": "^3.4.1",
    "node-sass": "^4.5.3",
    "nodemon": "^1.11.0",
    "onchange": "^3.2.1",
    "parallelshell": "^2.0.0"
  },
  "babel": {
    "presets": [
      "es2015",
      "stage-2"
    ],
    "plugins": []
  }
}
